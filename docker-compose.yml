services:
  life-stats:
    build: .
    container_name: life-stats-tracker
    ports:
      - "5001:5001"
    volumes:
      - ./data:/app/data
      - ./name_mappings.json:/app/name_mappings.json
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
